import 'package:flutter/material.dart';

class DoktorScreen extends StatelessWidget {
  const DoktorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
